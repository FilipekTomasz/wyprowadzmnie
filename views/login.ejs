<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wyprowadz mnie | Logowanie</title>
    <link rel="stylesheet" href="css/login.css">
    <link rel="Shortcut icon" href="image/logo.svg"> 
    <meta name="description" content="X"/> <!-- Do zrobienia -->
    <meta name="keywords" content="słowo_kluczowe 1, słowo_kluczowe 2"/>
    <meta name="author" content="Author name" />
    <meta name="copyright" content="Copyright owner" /> 
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.6.1/css/all.css">
</head>
<body>
    <main>
            <% if (message.length > 0){ %>
            <% if (message != 'Email został potwierdzony' && message != 'Hasło zostało zmienione' && message != 'Pomyslnie się wylogowałeś'){ %>
            <div class="notification">
                <h3><%= message %></h3>
            </div>
            <%} else {%>
            <div class="notification--green">
                <h3><%= message %></h3>
            </div>
            <%}%>
            <%}%>
            <div class="login">
                <h1 class="login__headline">wyprowadźmnie.com</h1>
                <form action="/login" method="POST" class="login__input">
                    <a href="/"><img alt="Logo wyprowadź mnie" src="image/pies.svg" class="login__input--logo"></a>
                        <h2 class="login__input--headline">Podaj email</h2>
                        <input class="login__input--email" type="text" id="email" name="email" required> 
                        <h2  class="login__input--headline">Podaj hasło</h2>           
                        <input class="login__input--password" type="password" id="password" name="password" required>
                    <button class="login__input--button" type="submit">Zaloguj</button>
                </form>
            </div>
            <a href="/resetpass" class="login__forgot"><h3>Zapomniałem hasła</h3></a><a href="/register" class="login__forgot"><h3>Zarejestruj się</h3></a>
    </main>
<script src="js/notification.js"></script>
</body>
</html>